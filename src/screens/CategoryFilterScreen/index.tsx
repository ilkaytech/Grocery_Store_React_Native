import React from "react";
import { ScrollView, Text } from "react-native";

const index = () => {
  return (
    <ScrollView>
      <Text>ilkay</Text>
    </ScrollView>
  );
};

export default index;
